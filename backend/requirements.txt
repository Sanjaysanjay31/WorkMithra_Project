# Direct dependencies only (transitive deps resolve via pip).
fastapi==0.131.0
uvicorn==0.41.0
# uvicorn's ws="auto" needs an installed websocket protocol implementation;
# without it every Socket.IO websocket upgrade fails on the deployed server
# (clients log "websocket error" and degrade to long-polling).
websockets==15.0.1
SQLAlchemy==2.0.46
psycopg2-binary==2.9.11
PyJWT==2.12.1
passlib==1.7.4
bcrypt==4.0.1
python-socketio==5.16.1
python-multipart==0.0.22
python-dotenv==1.2.1
email-validator==2.3.0
httpx==0.28.1
requests==2.32.5
slowapi==0.1.9

# Dev / test
pytest==9.0.3
